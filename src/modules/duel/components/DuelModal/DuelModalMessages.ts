import { INITIAL_CARDS_DRAWN_IN_DUEL } from 'src/modules/duel/duelConstants'

export const versusMessage = 'VS'
export const initialDrawMessage = `Both players draw ${INITIAL_CARDS_DRAWN_IN_DUEL} cards.`
export const firstPlayerMessage = 'goes first!'
export const victoryMessage = 'has won the duel!'
